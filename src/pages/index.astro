---
import Base from '../layouts/Base.astro'
import Footer from '../components/Footer.astro'
import DemandEngine from '../components/DemandEngine.astro'
import ProofCards from '../components/ProofCards.astro'

// Share-Line proof wall (home). Numbers sourced from share-line-job-counts-variable-growth.csv
// (jobs 2023 -> 2025 total growth). Verified, market-volume comparator flat-to-contracting.
const homeCards = [
  { trade: 'Well drilling',    loc: '',                      num: '9¢',   unit: 'per click',     market: 'in an industry that averages $7.85 a click', down: false, featured: true },
  { trade: 'Glass',            loc: '',                      num: '1 in 8', unit: 'clicks become leads', market: 'most campaigns convert 3–5%', down: false },
  { trade: 'Landscaping',      loc: '',              num: '+40%', unit: 'more jobs',     market: 'in a flat market', down: false },
  { trade: 'Roofing',          loc: '',              num: '+23%', unit: 'more jobs',     market: 'in a flat market', down: false },
  { trade: 'Plumbing',         loc: '',                      num: '1 in 3', unit: 'clicks become calls', market: 'in the off-season', down: false },
  { trade: 'Fencing',          loc: '', num: '+25%', unit: 'more jobs',     market: 'in a flat market', down: false },
  { trade: 'Gutters',          loc: '',   num: '+22%', unit: 'more jobs',     market: 'in a flat market', down: false },
  { trade: 'Windows & doors',  loc: '',                      num: '16.1%', unit: 'click-through rate', market: 'the industry benchmark is 6.47%', down: false },
  { trade: 'Pools',            loc: '',              num: '+26%', unit: 'more installs', market: 'while pools fell 50%', down: true },
  { trade: 'Metal roofing',    loc: '',               num: '+30%', unit: 'more sold',     market: 'while the market shrank 15–25%', down: true },
  { trade: 'Stone',            loc: '',              num: '+232%', unit: 'more demand',  market: 'in the off-season', down: false },
];
---
<Base
  title="Marketing Performance Group — Your Market's Jobs, Minus Your Competitor"
  description="The home-services market barely added a job in three years. The growth was price, not more work. Yet our clients booked double-digit more jobs, taken off their competitors' schedules. One company per trade, per market. 41 years in the industry, 31 running Marketing Performance."
>
  <!-- Minimal top nav. Logo only. Everything else moved to footer. -->
  <nav class="mp-nav">
    <div class="mp-nav-inner">
      <a href="/" class="mp-logo">
        <img src="/mp-logo-v3.png" alt="Marketing Performance — Powered by EngageEngine™" width="217" height="44" />
      </a>
    </div>
  </nav>

  <main class="mp">

    <!-- ═══ 1. HERO ═══ -->
    <section class="mp-hero">
      <div class="mp-hero-grid">
        <div class="mp-hero-copy">
          <h1 class="mp-h1">
            <span class="hero-word">Your </span>
            <span class="hero-word">market </span>
            <span class="hero-word">didn't </span>
            <span class="hero-word">grow </span>
            <span class="hero-word">last </span>
            <span class="hero-word">year.</span><br />
            <span class="hero-word">Somebody's </span>
            <span class="hero-word">schedule </span>
            <span class="hero-word">did.</span>
          </h1>
          <div class="mp-hero-prose hero-sub">
            <p>The growth everyone talks about is just prices going up. The number of actual jobs barely moved.</p>
            <p>So when the guy across town gets busier, that work is coming off your schedule. We put it back on yours, and lock him out. One company per trade, per market.</p>
          </div>

          <div class="hero-cta mp-hero-cta-block">
            <a href="/snapshot" class="mp-btn-primary">See if your trade is still open</a>
            <p class="mp-micro"><strong>41 years in the industry. 31 running Marketing Performance.</strong></p>
          </div>
        </div>

        <!-- Hero VSL. Autoplays muted; burned-in captions carry it, controls allow unmute. -->
        <figure class="mp-artifact mp-artifact-video">
          <div class="mp-hero-video">
            <video autoplay muted loop playsinline controls poster="/video/hub/hero-vsl-poster.jpg">
              <source src="/video/hub/hero-vsl.mp4" type="video/mp4" />
            </video>
          </div>
          <figcaption class="mp-artifact-cap">60 seconds. Why your phone isn't ringing.</figcaption>
        </figure>
      </div>
    </section>

    <!-- ═══ 1b. PROOF CARDS (Share Line) ═══ -->
    <ProofCards
      cards={homeCards}
      foot="Every one of those jobs came off a competitor's schedule. One company per trade, per market."
    />

    <!-- ═══ 2. PROBLEM ═══ -->
    <section class="mp-problem">
      <div class="mp-problem-inner">
        <p class="mp-eye" data-animate>The Problem</p>
        <h2 class="mp-h2" data-animate data-delay="0.05">The market isn't making new work. Everyone's fighting over the same jobs.</h2>
        <div class="mp-problem-prose" data-animate data-delay="0.1">
          <p>The number of homeowners hiring your trade barely moved in three years. The growth everyone talks about was just prices going up. So the work is basically fixed, and everybody is scrambling for the same slice.</p>
          <p>Angi and HomeAdvisor sell the same lead to four of you at once, so you race to dial first and quote against three guys who'll always go cheaper. Zip-code ads pay Google and Facebook to put you in front of renters, tire-kickers, and the guy who replaced his roof last spring.</p>
          <p>Here's the math nobody puts on the sales call. Fewer than <strong>3 in 100</strong> households will hire your trade this quarter. Old-way ads pay to reach all 100. Shared-lead platforms make you knife-fight over the 3.</p>
        </div>
        <p class="mp-problem-punch" data-animate data-delay="0.15">Both models get paid whether you close a job or not. You're not their customer. You're their inventory. Meanwhile the contractor who locked his market is quietly booking the jobs off everyone else's schedule.</p>
      </div>
    </section>

    <!-- ═══ 3. SOLUTION ═══ -->
    <section class="mp-solution">
      <div class="mp-solution-inner">
        <p class="mp-eye" data-animate>The Solution</p>
        <h2 class="mp-h2" data-animate data-delay="0.05">We lock your market. Then we point the work at you.</h2>
        <div class="mp-solution-prose" data-animate data-delay="0.1">
          <p><strong>One company per trade, per market.</strong> The day you claim yours, your competitor can't. That is the product, not a bonus.</p>
          <p>Here's how we deliver it. Every homeowner leaves a trail before they call anyone: the searches, the review pages, the three sites they check in a week. <strong>EngageEngine™</strong> reads who in your area is actually deciding right now and puts your ads in front of only them, on Google, on Facebook, on YouTube. You stop paying to reach the 97 who were never going to call and reach the few who will.</p>
          <p>And because the seat is yours alone, every job it produces is a job the guy across town didn't get. That is what taking your market looks like.</p>
        </div>
      </div>
    </section>

    <!-- ═══ 4. THE ENGINE (interactive mechanism reveal) ═══ -->
    <DemandEngine />

    <!-- Founder block removed 2026-07-17: off-aesthetic, belongs to a deeper page. -->
    <!-- Value Stack removed 2026-07-17: discounty price-anchor + names pitch, off the new positioning. -->

    <!-- ═══ 4b. WE CATCH IT BEFORE IT COSTS YOU ═══ -->
    <section class="mp-catch">
      <div class="mp-catch-inner">
        <p class="mp-eye" data-animate>After the Launch</p>
        <h2 class="mp-h2" data-animate data-delay="0.05">We catch it before it costs you.</h2>
        <p class="mp-catch-lede" data-animate data-delay="0.1">Campaigns don't fail loudly. They fail quietly, in ways you'd never see on an invoice. Every account gets watched every week. Recent catches:</p>
        <ul class="mp-catch-list" data-animate data-delay="0.15">
          <li>A plumbing client's campaign delivery collapsed 90% overnight. Caught the same day, diagnosed as a campaign issue, not a market issue, and restored — conversions never dipped.</li>
          <li>An email campaign started silently dropping one in four sends. Caught before the next send went out, fixed before the client ever saw it.</li>
          <li>Visitors on three client sites were clicking buttons that didn't respond — at the exact moment they were trying to act. Found it in the behavior recordings. Fixed it.</li>
        </ul>
        <p class="mp-catch-foot" data-animate data-delay="0.2">The launch is the easy part. The watching is what you're paying for.</p>
      </div>
    </section>

    <!-- ═══ 5. GUARANTEE ═══ -->
    <section class="mp-guarantee">
      <div class="mp-section-head mp-center">
        <p class="mp-eye" data-animate>The Guarantee</p>
        <h2 class="mp-h2" data-animate data-delay="0.05">The 2X Revenue Guarantee.</h2>
      </div>

      <div class="mp-guarantee-card" data-animate data-delay="0.1">
        <div class="mp-guarantee-badge">
          <span class="mp-badge-check">✓</span>
          <span>2X Revenue Guarantee</span>
        </div>
        <p class="mp-guarantee-lead">If the system doesn't put at least twice your investment back in your pocket as closed revenue within 30 days, we keep working for free until it does. The guarantee is on our fee, not your ad spend.</p>
        <p class="mp-guarantee-body">You risk nothing but the 30 seconds it takes to look at your market.</p>
      </div>
    </section>

    <!-- ═══ 7. FINAL CTA ═══ -->
    <section class="mp-close">
      <h2 class="mp-close-h1" data-animate>One company per trade, per market.</h2>
      <p class="mp-close-h2" data-animate data-delay="0.05">Is your territory still open?</p>
      <p class="mp-close-body" data-animate data-delay="0.1">Our clients buy clicks for nine cents in an industry that averages $7.85. That's what in-market data does.</p>
      <p class="mp-close-body" data-animate data-delay="0.15">We can't sell your market twice. That's the whole point. If your competitor claims it first, we start sending <em>him</em> the work.</p>
      <div class="mp-close-cta" data-animate data-delay="0.2">
        <a href="/snapshot" class="mp-btn-primary">See If My Trade Is Still Open</a>
        <p class="mp-micro">Free. No email required. Takes 30 seconds.</p>
      </div>
    </section>

  </main>
  <Footer />
</Base>

<style>
  /* ═══════════════════════════════════════════════════════
     APPLE-FIDELITY HOMEPAGE. Hormozi rewrite v2.
     Tokens: Canvas White #f5f5f7 / Pure White #ffffff /
     Midnight Graphite #1d1d1f / Mid Gray #707070 /
     Interactive Blue #0071e3 / Border Silver #d6d6d6.
     Cards 0px radius. Images 8px. Buttons 980px pill.
     Shadow XL: rgba(0,0,0,0.22) 3px 5px 30px 0px.
     ═══════════════════════════════════════════════════════ */

  /* ─── Top nav (logo only) ─── */
  .mp-nav {
    position: sticky; top: 0; z-index: 100;
    background: rgba(255,255,255,0.72);
    backdrop-filter: saturate(180%) blur(20px);
    -webkit-backdrop-filter: saturate(180%) blur(20px);
    border-bottom: 1px solid var(--border);
  }
  .mp-nav-inner {
    max-width: 1280px; margin: 0 auto;
    padding: 0 32px; height: 56px;
    display: flex; align-items: center;
  }
  .mp-logo {
    display: flex; align-items: center;
    min-height: 44px; text-decoration: none;
  }
  .mp-logo img { height: 44px; width: auto; display: block; }
  .mp-logo:hover { text-decoration: none; }
  @media (max-width: 600px) {
    .mp-logo img { height: 34px; }
  }

  /* ─── Page base ─── */
  .mp { background: var(--canvas); color: var(--graphite); }
  .mp h1, .mp h2 { font-family: var(--font-d); color: var(--graphite); }
  .mp p { color: var(--graphite); }
  .mp em { font-style: italic; }

  /* ─── Primary button (Apple Interactive Blue pill) ─── */
  .mp-btn-primary {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--blue); color: #fff;
    font-family: var(--font-t); font-size: 17px;
    font-weight: 400; letter-spacing: -0.022em;
    padding: 12px 24px; border-radius: 980px;
    text-decoration: none; transition: background 0.2s;
    line-height: 1.2;
  }
  .mp-btn-primary:hover { background: var(--blue-link); text-decoration: none; color: #fff; }
  .mp-btn-primary::after {
    content: "→"; font-size: 17px; line-height: 1;
    transition: transform 0.2s;
  }
  .mp-btn-primary:hover::after { transform: translateX(3px); }

  /* ─── Eyebrows + headings ─── */
  .mp-eye {
    font-family: var(--font-t);
    font-size: 12px; font-weight: 600;
    letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--blue); margin-bottom: 16px;
  }
  .mp-h1 {
    font-family: var(--font-d);
    font-size: clamp(36px, 4.4vw, 56px);
    font-weight: 600; line-height: 1.05;
    letter-spacing: -0.022em; color: var(--graphite);
    margin-bottom: 14px;
  }
  .mp-h2 {
    font-family: var(--font-d);
    font-size: clamp(30px, 3.8vw, 44px);
    font-weight: 600; line-height: 1.1;
    letter-spacing: -0.018em; color: var(--graphite);
    margin-bottom: 20px;
    max-width: 780px;
  }
  .mp-lede {
    font-family: var(--font-t);
    font-size: 21px; line-height: 1.4;
    letter-spacing: -0.022em; color: var(--mid-gray);
    max-width: 680px;
  }
  .mp-micro {
    font-family: var(--font-t);
    font-size: 13px; color: var(--light-gray);
    letter-spacing: -0.015em;
  }
  .mp-micro strong { color: var(--graphite); font-weight: 600; }
  .mp-section-head { margin-bottom: 48px; }
  .mp-section-head.mp-center,
  .mp-h2.mp-center,
  .mp-eye.mp-center { text-align: center; }
  .mp-section-head.mp-center .mp-h2,
  .mp-section-head.mp-center .mp-lede { margin-left: auto; margin-right: auto; }
  .mp-h2.mp-center { margin-left: auto; margin-right: auto; }

  /* ─── 1. HERO ─── */
  .mp-hero {
    padding: 80px 32px 100px;
    background: var(--canvas);
  }
  .mp-hero-grid {
    max-width: 1320px; margin: 0 auto;
    display: grid; grid-template-columns: 1fr; gap: 56px;
    align-items: center;
  }
  @media (min-width: 1024px) {
    .mp-hero-grid {
      grid-template-columns: minmax(360px, 520px) minmax(0, 1fr);
      gap: 64px;
    }
  }
  .mp-hero-copy { max-width: 540px; }
  .mp-hero-prose {
    display: flex; flex-direction: column;
    gap: 14px;
    max-width: 540px;
    margin-bottom: 32px;
  }
  .mp-hero-prose p {
    font-family: var(--font-t);
    font-size: 18px; line-height: 1.55;
    letter-spacing: -0.022em;
    color: var(--mid-gray);
  }
  .mp-hero-cta-block {
    display: flex; flex-direction: column;
    gap: 12px; align-items: flex-start;
  }

  /* Snapshot artifact. Actual product screenshot, framed Apple-style. */
  .mp-artifact {
    margin: 0;
    justify-self: stretch; width: 100%;
    display: flex; flex-direction: column; gap: 14px;
  }
  .mp-artifact-img {
    width: 100%; height: auto; display: block;
    border-radius: 8px;
    box-shadow: var(--shadow-xl, rgba(0,0,0,0.22) 3px 5px 30px 0px);
    background: var(--white);
  }
  .mp-artifact-cap {
    font-family: var(--font-t);
    font-size: 12px; font-weight: 400;
    letter-spacing: -0.015em;
    color: var(--light-gray);
    text-align: center;
  }
  @media (max-width: 1023px) {
    .mp-artifact { max-width: 100%; }
  }

  /* Hero VSL (replaces the divergence chart) */
  .mp-hero-video {
    width: 100%;
    background: #1d1d1f;
    border-radius: 8px;
    overflow: hidden;
    aspect-ratio: 16 / 9;
    box-shadow: var(--shadow-xl, rgba(0,0,0,0.22) 3px 5px 30px 0px);
  }
  .mp-hero-video video { width: 100%; height: 100%; display: block; }

  /* Proof-card styles now live in ProofCards.astro (shared by / and /snapshot). */
  @media (max-width: 768px) {
    /* Unlike the old chart, the video stays on mobile — 16:9 is short enough
       not to bury the proof cards. */
    .mp-hero { padding-bottom: 8px; }
  }

  /* ─── 4b. CATCH ─── */
  .mp-catch { padding: 110px 32px; background: var(--white); border-top: 1px solid var(--border); }
  .mp-catch-inner { max-width: 760px; margin: 0 auto; }
  .mp-catch-lede {
    font-family: var(--font-t); font-size: 19px; line-height: 1.5;
    letter-spacing: -0.02em; color: var(--mid-gray); margin-bottom: 34px;
  }
  .mp-catch-list { list-style: none; padding: 0; margin: 0 0 34px; }
  .mp-catch-list li {
    font-family: var(--font-t); font-size: 17px; line-height: 1.55;
    letter-spacing: -0.015em; color: var(--graphite);
    padding: 20px 0 20px 34px; position: relative;
    border-top: 1px solid var(--border);
  }
  .mp-catch-list li:last-child { border-bottom: 1px solid var(--border); }
  .mp-catch-list li::before {
    content: ''; position: absolute; left: 4px; top: 29px;
    width: 8px; height: 8px; border-radius: 50%;
    background: var(--blue, #0071e3);
  }
  .mp-catch-foot {
    font-family: var(--font-t); font-size: 17px; font-weight: 600;
    letter-spacing: -0.015em; color: var(--graphite);
  }

  /* ─── 2. PROBLEM ─── */
  .mp-problem {
    padding: 112px 32px;
    background: var(--white);
    border-top: 1px solid var(--border);
  }
  .mp-problem-inner {
    max-width: 780px; margin: 0 auto;
  }
  .mp-problem-prose {
    display: flex; flex-direction: column; gap: 20px;
    margin: 28px 0 36px;
  }
  .mp-problem-prose p {
    font-family: var(--font-t);
    font-size: 18px; line-height: 1.6;
    letter-spacing: -0.018em;
    color: var(--mid-gray);
  }
  .mp-problem-prose p strong {
    color: var(--graphite); font-weight: 600;
  }
  .mp-problem-punch {
    font-family: var(--font-d);
    font-size: clamp(20px, 2.4vw, 26px);
    font-weight: 500; line-height: 1.35;
    letter-spacing: -0.015em;
    color: var(--graphite);
    border-left: 3px solid var(--blue);
    padding-left: 20px;
    margin-top: 8px;
  }

  /* ─── 3. SOLUTION ─── */
  .mp-solution {
    padding: 112px 32px;
    background: var(--canvas);
    border-top: 1px solid var(--border);
  }
  .mp-solution-inner {
    max-width: 780px; margin: 0 auto;
  }
  .mp-solution-prose {
    display: flex; flex-direction: column; gap: 20px;
    margin-top: 28px;
  }
  .mp-solution-prose p {
    font-family: var(--font-t);
    font-size: 18px; line-height: 1.6;
    letter-spacing: -0.018em;
    color: var(--graphite);
  }
  .mp-solution-prose p strong {
    color: var(--blue); font-weight: 600;
  }
  .mp-solution-more {
    margin-top: 32px;
    font-family: var(--font-t);
    font-size: 15px;
    letter-spacing: -0.015em;
  }
  .mp-text-link {
    display: inline-flex;
    align-items: baseline; gap: 6px;
    color: var(--blue-link);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.15s;
  }
  .mp-text-link:hover { color: var(--blue); }
  .mp-text-link-arrow {
    display: inline-block;
    transition: transform 0.2s;
  }
  .mp-text-link:hover .mp-text-link-arrow {
    transform: translateX(3px);
  }

  /* ─── 4. PROOF ─── */
  .mp-proof-section {
    padding: 112px 32px;
    background: var(--white);
    border-top: 1px solid var(--border);
  }
  .mp-proof-block {
    max-width: 780px; margin: 0 auto;
  }
  .mp-proof-feature {
    display: flex; align-items: center; gap: 24px;
    padding: 28px 32px;
    background: var(--canvas);
    margin: 32px 0 40px;
    border-left: 2px solid var(--blue);
  }
  .mp-anchor-photo {
    width: 80px; height: 80px;
    border-radius: 50%;
    background: var(--canvas);
    border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    font-family: var(--font-d);
    font-size: 22px; font-weight: 600;
    color: var(--mid-gray);
    flex-shrink: 0;
    overflow: hidden;
  }
  .mp-anchor-photo img {
    width: 100%; height: 100%;
    object-fit: cover;
    display: block;
  }
  .mp-anchor-attr {
    font-family: var(--font-t);
    font-size: 14px; color: var(--mid-gray);
    margin-bottom: 8px; letter-spacing: -0.018em;
  }
  .mp-anchor-attr strong { color: var(--graphite); font-weight: 600; }
  .mp-anchor-stats {
    font-family: var(--font-t);
    font-size: 13px; color: var(--blue-link);
    letter-spacing: -0.015em;
    display: flex; flex-wrap: wrap; gap: 8px;
  }
  .mp-anchor-stats strong { font-weight: 600; }
  .mp-dot { color: var(--light-gray); }
  .mp-proof-stack {
    list-style: none; padding: 0; margin: 0;
    display: flex; flex-direction: column; gap: 22px;
  }
  .mp-proof-stack > li {
    font-family: var(--font-t);
    font-size: 17px; line-height: 1.55;
    letter-spacing: -0.018em;
    color: var(--graphite);
    padding-left: 24px;
    position: relative;
  }
  .mp-proof-stack > li::before {
    content: "→";
    position: absolute; left: 0; top: 0;
    color: var(--blue); font-weight: 700;
  }
  .mp-proof-stack > li strong { font-weight: 600; }
  .mp-proof-founder {
    display: flex; align-items: flex-start; gap: 18px;
    padding: 22px 24px;
    background: var(--canvas);
    border-left: 2px solid var(--blue);
  }
  .mp-proof-founder::before { display: none; }
  .mp-proof-founder-photo {
    width: 72px; height: 72px;
    border-radius: 8px;
    object-fit: cover; object-position: top;
    flex-shrink: 0;
    box-shadow: rgba(0,0,0,0.12) 2px 3px 14px 0px;
  }
  .mp-proof-founder-quote {
    font-family: var(--font-d);
    font-size: 17px; font-style: italic; line-height: 1.45;
    letter-spacing: -0.018em;
    color: var(--graphite);
    margin-bottom: 8px;
  }
  .mp-proof-founder-attr {
    font-family: var(--font-t);
    font-size: 13px; color: var(--mid-gray);
    letter-spacing: -0.015em;
  }
  .mp-proof-founder-attr strong { color: var(--graphite); font-weight: 600; }
  @media (max-width: 640px) {
    .mp-proof-feature { flex-direction: column; align-items: flex-start; }
    .mp-proof-founder { flex-direction: column; }
  }

  /* ─── 5. VALUE STACK ─── */
  .mp-offer {
    padding: 112px 32px;
    background: var(--canvas);
    border-top: 1px solid var(--border);
  }
  .mp-offer-inner {
    max-width: 820px; margin: 0 auto;
  }
  .mp-stack {
    list-style: none; padding: 0;
    margin: 40px 0 32px;
    display: flex; flex-direction: column; gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
  }
  .mp-stack-item {
    background: var(--white);
    padding: 32px 36px;
    display: flex; flex-direction: column; gap: 12px;
  }
  .mp-stack-head {
    font-family: var(--font-d);
    font-size: 20px; font-weight: 600;
    letter-spacing: -0.018em; line-height: 1.3;
    color: var(--graphite);
  }
  .mp-stack-body {
    font-family: var(--font-t);
    font-size: 16px; line-height: 1.55;
    letter-spacing: -0.018em;
    color: var(--mid-gray);
    margin: 0;
  }
  .mp-stack-body strong { color: var(--graphite); font-weight: 600; }
  .mp-stack-anchor {
    font-family: var(--font-t);
    font-size: 14px;
    letter-spacing: -0.015em;
    color: var(--light-gray);
    font-style: italic;
    margin-top: 6px;
    padding-top: 12px;
    border-top: 1px solid var(--border);
  }
  .mp-stack-anchor strong {
    color: var(--blue); font-weight: 700; font-style: normal;
    font-size: 15px;
  }

  /* Compliance emphasis line */
  .mp-stack-emphasis {
    color: var(--graphite) !important;
    padding: 12px 16px;
    background: var(--canvas);
    border-left: 3px solid var(--blue);
    margin: 4px 0;
  }
  .mp-stack-emphasis strong em {
    font-weight: 600;
    font-style: italic;
  }

  /* Invoice image inside Engine 1 — small proof, not hero */
  .mp-stack-invoice {
    margin: 6px 0 2px;
    display: flex; flex-direction: column; gap: 6px;
    align-items: center;
  }
  .mp-stack-invoice-img {
    width: 100%; max-width: 240px; height: auto; display: block;
    border-radius: 4px;
    border: 1px solid var(--border);
    box-shadow: rgba(0,0,0,0.08) 0 2px 8px 0;
  }
  .mp-stack-invoice-cap {
    font-family: var(--font-t);
    font-size: 11px; color: var(--light-gray);
    letter-spacing: -0.015em;
    font-style: italic;
    text-align: center;
  }

  /* Total-value block at end of stack — retail anchor, NOT the fee */
  .mp-stack-total {
    text-align: center;
    margin: 32px auto 0;
    font-family: var(--font-t);
    letter-spacing: -0.015em;
    color: var(--mid-gray);
    max-width: 720px;
    padding: 32px 36px;
    background: var(--white);
    border-left: 3px solid var(--blue);
    display: flex; flex-direction: column;
    align-items: center; gap: 14px;
  }
  .mp-stack-total-head {
    font-family: var(--font-d);
    font-size: clamp(20px, 2.2vw, 24px);
    font-weight: 600;
    letter-spacing: -0.018em;
    line-height: 1.3;
    color: var(--graphite);
    margin: 0;
  }
  .mp-stack-total-body {
    font-family: var(--font-t);
    font-size: 16px;
    line-height: 1.55;
    letter-spacing: -0.015em;
    color: var(--mid-gray);
    max-width: 620px;
    margin: 0;
  }
  .mp-stack-total-body strong {
    color: var(--graphite); font-weight: 600;
  }
  .mp-stack-total-strike {
    position: relative;
    display: inline-block;
    font-family: var(--font-d);
    font-weight: 600;
    color: var(--light-gray);
    padding: 0 4px;
  }
  .mp-stack-total-strike::after {
    content: "";
    position: absolute;
    left: 0; right: 0;
    top: 50%;
    height: 2px;
    background: #d62828;
    transform: rotate(-6deg);
    transform-origin: center;
  }
  .mp-stack-total-close {
    font-family: var(--font-t);
    font-size: 15px;
    line-height: 1.5;
    letter-spacing: -0.015em;
    color: var(--mid-gray);
    padding-top: 14px;
    border-top: 1px solid var(--border);
    width: 100%;
    max-width: 560px;
    margin: 0;
  }
  .mp-stack-total-close strong {
    color: var(--blue); font-weight: 700;
  }

  /* ─── 6. GUARANTEE ─── */
  .mp-guarantee {
    padding: 112px 32px;
    background: var(--white);
    border-top: 1px solid var(--border);
  }
  .mp-guarantee .mp-section-head {
    max-width: 720px; text-align: center;
    margin: 0 auto 40px;
  }
  .mp-guarantee-card {
    max-width: 720px; margin: 0 auto;
    background: var(--canvas);
    border: 1px solid var(--border);
    padding: 44px 48px;
  }
  .mp-guarantee-badge {
    display: inline-flex; align-items: center; gap: 10px;
    background: var(--blue); color: #fff;
    padding: 8px 18px; border-radius: 999px;
    font-family: var(--font-t);
    font-size: 13px; font-weight: 600;
    letter-spacing: 0.02em;
    margin-bottom: 24px;
  }
  .mp-badge-check {
    width: 18px; height: 18px; border-radius: 50%;
    background: #fff; color: var(--blue);
    display: inline-flex; align-items: center; justify-content: center;
    font-size: 11px; font-weight: 700;
  }
  .mp-guarantee-lead {
    font-family: var(--font-d);
    font-size: clamp(22px, 2.4vw, 26px);
    font-weight: 500; line-height: 1.35;
    letter-spacing: -0.018em;
    color: var(--graphite); margin-bottom: 16px;
  }
  .mp-guarantee-body {
    font-family: var(--font-t);
    font-size: 17px; line-height: 1.55;
    letter-spacing: -0.022em;
    color: var(--mid-gray);
  }

  /* ─── 7. CLOSE ─── */
  .mp-close {
    padding: 140px 32px 128px;
    background: var(--canvas);
    border-top: 1px solid var(--border);
    text-align: center;
  }
  .mp-close-h1 {
    font-family: var(--font-d);
    font-size: clamp(32px, 4.6vw, 48px);
    font-weight: 600; line-height: 1.1;
    letter-spacing: -0.018em;
    color: var(--graphite); margin-bottom: 4px;
    max-width: 780px; margin-left: auto; margin-right: auto;
  }
  .mp-close-h2 {
    font-family: var(--font-d);
    font-size: clamp(32px, 4.6vw, 48px);
    font-weight: 600; line-height: 1.1;
    letter-spacing: -0.018em;
    color: var(--blue); margin-bottom: 28px;
  }
  .mp-close-body {
    font-family: var(--font-t);
    font-size: 19px; line-height: 1.5;
    letter-spacing: -0.022em;
    color: var(--mid-gray);
    max-width: 640px; margin: 0 auto 36px;
  }
  .mp-close-cta {
    display: flex; flex-direction: column;
    align-items: center; gap: 10px;
  }

  /* ─── Responsive ─── */
  @media (max-width: 768px) {
    .mobile-hide { display: none; }
    .mp-hero { padding: 56px 22px 72px; }
    .mp-problem, .mp-solution, .mp-proof-section, .mp-offer, .mp-guarantee { padding: 72px 22px; }
    .mp-close { padding: 88px 22px 80px; }
    .mp-guarantee-card { padding: 32px 26px; }
    .mp-stack-item { padding: 24px 22px; }
    .mp-proof-feature { padding: 22px 24px; }
    .mp-proof-founder { padding: 20px 24px; }
    .mp-stack-total { padding: 20px 22px; }
  }
</style>

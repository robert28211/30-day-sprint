{
  "enabled": false,
  "auto_apply_confidence": "high",
  "git": {
    "commit": true,
    "push": false,
    "_push_note": "Repo root is the home dir (/Users/robbiebutt), remote 'gbp' = gbp-intelligence. Local commits give full history + rollback. Leave push false unless you want skill learnings pushed to that shared remote."
  },
  "target": {
    "mode": "auto-detect",
    "skill": null,
    "_comment": "mode: 'auto-detect' reflects into whichever skill ran this session. To pin reflect to the daily client pipeline skill once it ships, set mode to 'pinned' and skill to the skill folder name, e.g. 'daily-pipeline'."
  },
  "write_strategy": "learnings-section",
  "_write_strategy_options": "learnings-section = append/update a '## Learnings' block inside the target SKILL.md (safe, never touches the procedure body). body-edit = allow direct edits to procedure steps (only on explicit approval, never in auto mode)."
}
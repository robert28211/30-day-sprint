# Global Learnings
<!-- Maintained by /reflect. Newest at top. Each line: - [TIER] (YYYY-MM-DD) statement -->

- [MED] (2026-06-17) When `engram_checkpoint` errors (returned 404 repeatedly this session — server-side, not the data), fall back to `engram_remember` with the same summary content. Never drop session context just because that one tool is down — remember carries the same payload durably.
- [MED] (2026-06-17) Before editing/deploying a Cloudflare Worker, confirm which branch is ACTUALLY deployed — the live code may run from a `claude/*` worktree branch, NOT git `main`. gbp-intelligence's deployed code lives on `claude/intelligent-cohen` (worktree `~/.claude/worktrees/intelligent-cohen/`); `main` was 3 months stale and missing the entire DataForSEO/posts feature. Verify by matching a verbatim live-site string to the candidate source before assuming you're editing the right code.
- [MED] (2026-06-17) After deploying, `curl` the EXACT domain the user says they use to confirm it resolves to the worker before claiming the fix is live — the user's stated URL may be wrong. This session Robbie said the tool was at `gbp.engageengine.ai`, but that returns 000 (does not resolve); only `gbp.engageengine.cc` serves the worker. Catch the bad domain before telling them "it's live."
- [HIGH] (2026-06-14) When the user points to a URL/repo/file and asks "will this help / should we use it," READ the actual source IN FULL before any verdict — never assess from the URL, name, or priors. Assessing-from-the-name is the exact "guessing" Robbie is sick of; he flagged it explicitly this session.
- [HIGH] (2026-06-14) Answer yes/no questions with the answer in the FIRST word, then a tight verdict. Cut sermons, hedging, "it depends" non-answers, restating the user's question, and selling things they didn't ask for. Robbie: "are you just writing a goddamn book?" — brevity is respect, the direct answer comes first.
- [HIGH] (2026-06-14) DO the obvious reversible edit in the SAME turn instead of proposing it or asking "want me to show you the text?" — proposing-instead-of-doing reads as stalling and is the same problem Robbie hates. The session Stop hook explicitly rejected a turn that proposed but didn't implement. Confirm only for genuinely irreversible / outward-facing actions.
- [MED] (2026-06-14) When incorporating an external operating-doc/ruleset, add ONLY the net-new deltas and skip whatever duplicates existing rules — don't paste the whole doc (it bloats always-loaded context). This session: from Fable5.md (github.com/sgup/ai) added only 4 of ~12 items; the rest duplicated already-installed gates.
- [MED] (2026-06-14) Standing operating gates 1–8 + anti-proselytizing gate 5 + a "match effort to blast radius" default now live at the TOP of ~/.claude/CLAUDE.md (auto-loaded every session), installed this session from github.com/lerugray/capture-before-the-gate + github.com/sgup/ai Fable5.md. They cover: inspect-path-before-delete, prior-art-before-build, verify-negative-claims, read-before-verdict, label-confirmed-vs-inferred, baseline-before-"no-regressions", old-contract check. Don't re-derive them.
- [HIGH] (2026-06-14) Before `wrangler pages deploy --project-name X` (or trusting a worktree's `wrangler.toml` Pages `name`), VERIFY what X currently serves — `curl https://X.pages.dev` + check the project's custom domains. A Cloudflare Pages project can be the live home of an UNRELATED production domain, and a deploy silently OVERWRITES it. The marketingperformance.net Astro worktree (`youthful-tu`) had `name = "engageengine-staging"`, which serves the LIVE engageengine.ai site → MPN staging deploys overwrote engageengine.ai for ~2 days. Fixed: MPN staging now has its own project `marketingperformance-staging`; engageengine.ai stays on `engageengine-staging`. Full detail: memory/incident_engageengine_ai_overwrite.md. This is the standing "named target + overwrite verb → check what it actually is, this turn, before acting" gate applied to deploys.
- [HIGH] (2026-06-14) Production-incident protocol when the user says "you broke/overwrote X": STOP deploying, investigate READ-ONLY first (curl the live URL, list Cloudflare deploy/version history), restore to a known-good via ROLLBACK, THEN fix root cause. Cloudflare keeps full history — Pages: `POST /accounts/{acct}/pages/projects/{proj}/deployments/{id}/rollback`; Workers: `wrangler rollback`. Do NOT roll back / redeploy on an incomplete mental model — acting before understanding the wiring is what caused the original damage and risks compounding it. Be accurate about cause (own what's yours, correct the record where the evidence differs) without being defensive.
- [MED] (2026-06-14) Cloudflare restore facts: Pages keeps EVERY deployment, so restoring an overwritten Pages site = re-promote a prior deployment (no rebuild, no source needed). The "Edit Cloudflare Workers" token template grants Pages:Edit (read projects + roll back) but NOT Zone:DNS:Read — so it can't see or repoint apex DNS; DNS / custom-domain fixes need a Zone:DNS:Edit token or the dashboard.

- [HIGH] (2026-06-08) Sprint Tracker — trust "it worked before": when the user says something "worked in a previous deployment," never conclude it was never written. Trust them and investigate git history + Cloudflare deployment history (wrangler deployments list) first. This session I wrongly said the maintenance CSS "was never written" — it had been deployed but the subsequent Health tab commit (6296dee, +676/-676 lines) silently dropped it because it was never committed to git.
- [HIGH] (2026-06-08) Sprint Tracker — wrangler.toml is mandatory: deploying `engageengine-sprint` without a `wrangler.toml` strips all D1 bindings → `env.DB` is undefined → every API route throws "Cannot read properties of undefined" → all pages blank. ALWAYS deploy from `~/Desktop/engageengine-sprint/` with the toml present. Spec: `name="engageengine-sprint"`, `main="src/worker.js"`, D1 binding `DB` → `command-center` (id `13f98f2a-574d-495c-acd7-4f8900acd036`).
- [MED] (2026-06-08) Sprint Tracker — home-dir git force-add: always use `git -C ~ add -f Desktop/engageengine-sprint/src/worker.js` for Sprint Tracker files. The home-dir repo (`~`) has `/*` in `.gitignore` — plain `git add` silently does nothing. This is the same machine-wide rule as `.claude/` files (see 2026-06-11 HIGH entry) applied to Desktop/ paths.
- [MED] (2026-06-08) Sprint Tracker — auth credentials: APP_PASSWORD = `Rb123456++`. Auth flow: POST `/api/auth/login` with `{"password":"Rb123456++"}` → HMAC-SHA256 session cookie. Worker name: `engageengine-sprint`. URL: `https://engageengine-sprint.robertlbutt.workers.dev`.

- [HIGH] (2026-06-12) marketingperformance.net (the live EngageEngine site) is an **Astro** project living in the worktree `~/.claude/worktrees/youthful-tu` (branch `claude/youthful-tu`); the homepage is `src/pages/index.astro`. Ship it with `npm run build` (→ `dist/`) then `npx wrangler deploy --config wrangler-worker.toml` — worker `rough-brook-782b`, `main=dist/_worker.js/index.js`, `[assets]=dist`, bindings D1 `DB` (marketingperformance-leads) + R2 `PDFS` + `ASSETS`, secrets (Resend, TWILIO_*) persist automatically, and the custom domain is NOT in the toml so it persists across deploys. The live homepage is served as a STATIC ASSET, so the deployed `worker.js` you can pull from the CF API is a minified BUILD ARTIFACT — NEVER hand-edit it, the `mp-worker` GitHub repo (just an artifact backup), or the stale `homepage-apple.html` draft in the `goofy-gauss` worktree. Robbie's "go = ship" workflow runs from `youthful-tu`. (Cost me ~15 tool calls of spelunking through wrong repos this session.)
- [HIGH] (2026-06-12) When the user names where the work/source actually lives (a specific worktree, repo, or file), GO THERE DIRECTLY and STOP broad machine-wide filesystem searches. This session I kept `grep -r`-ing the whole home dir after Robbie had already pointed me at the worktree — he rejected the search tool call and corrected me ("every change I made recently was made in that worktree"). Trust the pointer over autonomous re-discovery.
- [MED] (2026-06-12) For a visual/interactive UI feature, the flow that worked end-to-end and got approved at every gate: (1) build a self-contained interactive HTML mockup in /tmp or home, (2) verify it with the `Claude_Preview` static server + screenshots (start `python3 -m http.server`, `preview_eval` to navigate, `preview_screenshot`), iterate to approval, (3) splice it into the real source NAMESPACED (prefix every class/selector with a unique root like `.ds-band` so nothing collides with the host page; in Astro use `<style is:global>` so runtime-injected innerHTML is styled, and `<script is:inline>` so the JS ships verbatim), (4) rebuild and verify the bundled CSS contains your rules before deploying. Splice large blocks with a Python line-range script that asserts boundary markers + writes a `.bak`, not fragile multi-line Edit matches.

- [HIGH] (2026-06-11) SQL `GROUP BY (col = value)` is a boolean-bucket trap: it collapses every non-matching row into ONE group and the SELECTed `col` shows an ARBITRARY representative, NOT a shared value. Reading "all rows = X" from it is wrong — this caused a false "health scores are a degenerate flat 67" diagnosis that led to shipping an unnecessary quarantine. Always `GROUP BY col` (the real column) for a true distribution before concluding "all X are Y."
- [HIGH] (2026-06-11) Committing to `.claude/` requires `git add -f`. `.claude` is gitignored in the home repo (`~`, remote gbp-intelligence) but the learnings/memory files are force-tracked. `git -C ~ add .claude/...` fails with "paths are ignored", and `~/.claude` is NOT its own repo (`git -C ~/.claude` resolves up to `~`). Correct sequence: `cd ~ && git add -f <path> && git commit -- <path>`. (This corrects reflect's Step 5 for this machine.)
- [MED] (2026-06-11) Neutral-prior shrinkage for composite scores over variable-coverage signals: blend measured signals with a neutral prior weighted like ~1 signal so sparse data can't reach a confident 0/100 (e.g. `(weighted_sum + 65*15)/(total_weight + 15)`). Separate CONFIDENCE (keyed off absolute scored-signal count — need ≥4 for "high", not just % coverage) from the SCORE. Emit an explicit "not measured" sentinel when zero signals have a baseline — never a default midpoint like 50.
- [MED] (2026-06-11) After changing a scoring METHOD, delete the pre-change historical score rows. Otherwise trend/direction calculations read a false "down/up" comparing new-method scores against old-method scores until the rolling window flushes (~7 days). Also neutralize the current run's artifact trends to flat.

- [HIGH] (2026-06-11) Cloudflare Workers AI free tier = 10,000 neurons/DAY (error 4006 when exhausted). A full bge-base embedding index of a 1,500-note vault exceeds this, so semantic-search indexing needs Cloudflare Workers Paid ($5/mo). Robbie upgraded. NEVER run an unbounded embedding loop on free tier — and never write a batch loop that can re-process the same items (it silently burns the whole daily quota).
- [HIGH] (2026-06-11) `env.AI.run` (Workers AI) throws transient `3043: Internal server error` on random batches. Embedding helpers MUST be resilient: sub-batch small (~10), retry once, then fall back to one-at-a-time and return null for failures — never throw, or a whole note/batch is lost. Separately, ONE non-finite value (NaN/null) in a vector makes Vectorize reject the ENTIRE upsert batch (VECTOR_UPSERT_ERROR 40023 "line None was not expected format") — validate every vector (correct dimension, all finite numbers) and upsert with a one-by-one fallback so one bad vector can't kill the batch.
- [MED] (2026-06-11) Cloudflare Vectorize `vectorCount` is eventually-consistent and lags behind upserts — don't treat a low mid-index count as failure. Drive batch indexing from a Python loop that advances offset by processed-count and retries on error; a naive bash loop that resets offset on an empty/error response will oscillate and re-process the same batch forever.
- [MED] (2026-06-11) BGE retrieval pattern: embed passages plain, but prefix QUERIES with "Represent this sentence for searching relevant passages: " before embedding. Operations Hub semantic vault search uses `@cf/baai/bge-base-en-v1.5` (768-dim) + Vectorize index `vault-knowledge`, exposed as the `search_vault` chat tool. See memory project_ops_hub_semantic_search.

- [HIGH] (2026-06-11) Cloudflare bot-mitigation 403s the default Python `urllib` User-Agent (`Python-urllib/x.y`) on the *.workers.dev endpoints — curl works, urllib silently fails. ALWAYS set a browser User-Agent header (`"User-Agent": "Mozilla/5.0 ..."`) on every urllib request to a Cloudflare Worker. This silently broke the reminder-completion drain for weeks (POST-backs 403'd, queue never drained).
- [HIGH] (2026-06-11) Bash here-docs with an UNQUOTED delimiter (`python3 << PYEOF`) make bash process backslashes and expand variables inside the body BEFORE Python sees it — `replace('\\\\','')` becomes `replace('\\','')` (SyntaxError), regex `\d` gets mangled. ALWAYS quote the delimiter (`<< 'PYEOF'`) and pass data in via environment variables (`VAR="$X" python3 << 'PYEOF'` then `os.environ["VAR"]`). The reminder drain crashed on EVERY run since written because of this.
- [HIGH] (2026-06-11) Operations Hub local scripts RUN from `~/bin/` (e.g. `~/bin/reminders-sync.sh`), NOT the repo copy in `command-center/`. The two DIVERGE — the repo copy can be an older variant missing features the deployed one has. ALWAYS check the LaunchAgent plist's ProgramArguments for the real path and edit THAT file. Both I and a workflow investigation agent wrongly diagnosed "feature missing" by reading the stale repo copy. Verify deployed-vs-repo before concluding anything about a local script.
- [SUPERSEDED 2026-06-11 → engine rebuilt, quarantine lifted same day; "degenerate flat 67" was a GROUP BY-trap misread. Real bug = fabricated-50 default + no confidence separation. Engine lives in GitHub repo client_health_report (Action writes shared D1). See memory project_ops_hub_next_health_rebuild.] (2026-06-11) Operations Hub client_health_scores engine is DEGENERATE — latest run scored 23 of 24 clients an identical 67, one at 0 (0 = failed data load, not a dying client). It is QUARANTINED in the brief as of 2026-06-11 (worker.js clientHealthStr detects degeneracy → emits "HEALTH SCORING QUARANTINED" and the brief ignores scores). The scoring engine needs a proper rebuild in a dedicated session — until then health scores must not drive findings.
- [HIGH] (2026-06-11) Operations Hub brief was manufacturing alarm from (a) open-items created with a date-stamped key so the same issue re-logged every morning with no dedup → 87-item pileup; (b) treating absence-of-data across sources as a compounding "clean zero" crisis; (c) logging infra/non-client labels ("Data Infrastructure", "AT&T", connector errors) as client alarms. Fixed: dedup (one open brief-item per client, refresh in place), 12-day auto-expiry, NON_CLIENT label guard, and an "absence is not evidence / 0 = data problem not client problem" rule in synthesisPrompt. Backlog of 87 archived.
- [MED] (2026-06-11) Operations Hub and the Sprint Tracker (engageengine-sprint worker) SHARE ONE D1 database (id 13f98f2a-574d-495c-acd7-4f8900acd036). The hub's `env.DB` already reaches sprint_jobs/sprint_tasks — completing a Sprint task from the hub is a same-DB UPDATE, not a cross-service integration. Status strings must be EXACTLY 'Complete'/'Active'/'Not Started' or handleAllWork filters drop the rows.
- [MED] (2026-06-11) Operations Hub brief generation: on error it previously saved the error text AS the morning brief with status='ready'. Now writes status='error' and won't clobber an existing good brief. Brief API temperature pinned to 0.2 for consistency.

- [HIGH] (2026-06-11) command-center (Operations Hub) worker.js serves ALL client-side JS inside a backtick template literal in `chatPage()`. Backslash escapes silently corrupt the served HTML and kill the entire `<script>` block (dead buttons, empty sidebar, no JS at all). NEVER use regex literals with `\` escapes — `/\*...\*/g` collapses to `/*...*/g` which JS reads as a block comment, leaving the trailing flag dangling → "g is not defined". NEVER put `\n`/`\t` in single-quoted strings (→ literal newline breaks the parse). NEVER use `\'` (→ bare apostrophe terminates the string). Use `new RegExp('[*]([^*]+)[*]','g')` with character classes (like the existing `renderMd()` does), `String.fromCharCode(96)` for backtick, `&rsquo;` HTML entities, and `' | '`/`String.fromCharCode(10)` for separators. One mangled token kills everything. Chrome "working" while Firefox/mobile broke = stale cache masking the bug; always wrangler deploy + hard refresh (Cmd+Shift+R). Burned 3× in one session.
- [HIGH] (2026-06-11) Operations Hub persona is JARVIS again as of 2026-06-11 (madhvantyagi/SOUL.md/souls/jarvis/SOUL.md) — REVERSES the 2026-06-08 Gojo switch. Composed/measured register, "Sir" address, anticipatory framing, no emoji/exclamations. Lives in 3 places in worker.js: SYSTEM_PROMPT personality block, synthesisPrompt tone section, brief API system message string — then wrangler deploy.
- [MED] (2026-06-11) Operations Hub voice (ElevenLabs TTS) working config: model `eleven_flash_v2`, voice ID `uS55xxE575Gfeg2Z4SFm` (PiVoice6v2), stability 0.5, similarity_boost 0.75. Server-side `/api/tts` proxy (key in ELEVENLABS_API_KEY secret), auto-plays after each assistant message, header Voice On/Off toggle persisted in localStorage. flash_v2 chosen for natural pacing without multilingual_v2's ~1s added latency.

- [HIGH] (2026-06-11) When building a Hormozi Grand Slam Offer for a service business, "free estimate" is not an offer — it's table stakes. The GSO must stack: (1) a dominant bonus the customer gets before paying anything, (2) specific risk reversals with real money behind them, and (3) a retention hook. Purely defensive positioning ("no surprises") is risk-reversal only — not a GSO.
- [HIGH] (2026-06-11) GA4 and Google Ads client access requests go to marketingperformancemcc@gmail.com, NOT robertlbutt3@gmail.com.
- [HIGH] (2026-06-11) Always clarify the trigger event before writing an email drip. "Cold/warm intent list" (behavioral signal, never contacted) requires a completely different sequence than "post-form-fill" (already raised hand). Never assume post-appointment context without confirming.
- [MED] (2026-06-11) Punch List Pros — Done-Right Guarantee approved elements: ✅ Free whole-home walkthrough at every estimate (anchor bonus) ✅ 2-hour arrival window or $100 off ✅ Nothing moves without customer approval ✅ Materials at cost, no markup ✅ 12-month workmanship warranty (quality/workmanship only, not preference) ✅ Priority scheduling for returning customers. NEVER use: "done in one day / day 2 free" (multi-day jobs common, liability), "price you quote is price you pay" absolute (change orders happen), "15 min fixes free" (minimum charge policy), satisfaction guarantee on color/preference choices.
- [MED] (2026-06-11) Punch List Pros copy: deck repairs are high-volume, high-margin — always feature prominently. Drywall is typically multi-visit (mud drying time) — never promise same-day completion for drywall.
- [HIGH] (2026-06-11) Before uploading demo videos showing real user data to public Meta ads, verify all personal information (names, addresses, phone numbers) is properly blurred. The Columbia SC roofing video incident showed unblurred names publicly for several hours — a serious privacy violation that must be caught at upload time, not after the ad is live.
- [HIGH] (2026-06-08) Operations Hub Anthropic calls in Cloudflare Workers: always add `if (!apiResp.ok) throw new Error(\`Anthropic \${apiResp.status}: \${JSON.stringify(data)}\`)` immediately after `await apiResp.json()` — without this, non-2xx responses silently produce undefined content and empty output with no error logged anywhere.
- [HIGH] (2026-06-08) Operations Hub brief AbortController timeout (22s) is calibrated for max_tokens ≤ 2000. At 4000 tokens Haiku takes >22s and aborts with "The operation was aborted." Never raise max_tokens above 2000 without also raising the abort timeout — or move generation to the cron trigger (15min wall-clock) instead of HTTP waitUntil (30s total).
- [MED] (2026-06-08) To debug empty Operations Hub brief: query BOTH `narrative_text` AND `action_items_json`. If action_len > 0 but narrative_len = 0, the output format section ordering is the issue (not an API failure). If both are 0, it's an API/auth/timeout error.
- [HIGH] (2026-06-08) Operations Hub morning brief is FULLY WORKING as of 2026-06-08 (deployed 322866fa). Four-bug chain: ctx missing → too many subrequests → no apiResp.ok check → ACTION_ITEMS before NARRATIVE ate all tokens. Final fix: NARRATIVE first in output format, max_tokens:2000. Brief generates real content (narrative_len~1853, action_len~2000). Cron: 0 10 * * * UTC. Manual trigger: POST /api/generate-brief from authenticated browser session.
- [HIGH] (2026-06-08) Operations Hub brief token budget fix: putting [ACTION_ITEMS] before [NARRATIVE] in the prompt caused action items to consume all tokens leaving narrative empty. Always put [NARRATIVE] FIRST in the output format so the most critical section is written before token budget runs out.
- [SUPERSEDED 2026-06-11 → Jarvis restored] (2026-06-08) Operations Hub persona is Gojo (madhvantyagi/SOUL.md/souls/gojo). Jarvis replaced. Future persona changes require updating 4 places in worker.js: SYSTEM_PROMPT personality block, synthesisPrompt tone section, brief API system message string, and UI greeting strings — then wrangler deploy.
- [HIGH] (2026-06-08) Operations Hub D1 `daily_briefs` table columns are `narrative_text`, `action_items_json`, `generated_at`, `status` — NOT `narrative` or `action_items`. Wrong column names return 400 SQLITE_ERROR.
- [HIGH] (2026-06-08) Operations Hub morning brief "Too many subrequests" bug — RESOLVED. Fixed by rewriting searchEmails to use gmailBatchMetadata (batch API). This was bug #2 in the four-bug chain; the brief is now fully working.
- [MED] (2026-06-08) `/api/generate-brief` requires session cookie auth (checkAuth), not the sync token. Cannot trigger via curl. Must use the browser (logged-in session) or call `regenerateBrief()` via browser JS on the authenticated tab.
- [MED] (2026-06-08) When polling for Operations Hub brief readiness, use browser-side JS (`fetch('/api/daily-brief')` from the authenticated tab) — the endpoint is behind auth and curl returns empty/wrong data.
